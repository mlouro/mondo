'use strict';


var config = {
  // Recurring options.
  'recurring': {
    'once': 'Just once',
    '1week': 'Every week',
    '2weeks': 'Every two weeks',
    '1month': 'Every month'
  }
}

module.exports = config;